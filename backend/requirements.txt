# Core LangGraph / LangChain
langgraph
langchain
langchain-community   # For FAISS tool integration and fallback imports
langchain-openai   # For OpenAI models (and Groq, which uses OpenAI-compatible API)

# LLM Provider Support (pre-install all for flexibility - allows switching models without redeploy)
langchain-google-genai   # For Google Gemini models (gemini-1.5-flash, gemini-1.5-pro, etc.)
langchain-ollama   # For Ollama local models (llama3.2, etc.)

# Vectorstore
faiss-cpu   # Or faiss-gpu if using GPU

# OpenAI embeddings
openai

# HTTP requests
requests

# Optional
typing-extensions

# Environment variables
python-dotenv

# MCP support
mcp>=1.20.0
langchain-mcp-adapters
fastmcp>=2.13.0.2
httpx>=0.28.1

# FastAPI and Web Server
fastapi
uvicorn[standard]
python-multipart

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0  # Direct bcrypt installation for better compatibility
python-multipart

# Rate limiting
slowapi>=0.1.9

# Encryption for sensitive data (MCP API keys)
cryptography>=41.0.0

# Document processing for RAG
pypdf2>=3.0.0  # PDF processing
python-docx>=1.1.0  # DOCX processing
unstructured>=0.10.0  # General document parsing
chardet>=5.0.0  # Character encoding detection

# Advanced RAG features
sentence-transformers>=2.2.0  # For re-ranking and embeddings
rank-bm25>=0.2.2  # BM25 for hybrid search
numpy>=1.24.0  # For vector operationsasync-lru
