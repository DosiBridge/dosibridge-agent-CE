# ============================================
# Backend Environment Variables Configuration
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env
#
# Or generate keys automatically:
# python3 backend/generate_keys.py
# ============================================

# ============================================
# REQUIRED VARIABLES
# ============================================

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Example: postgresql://sazib:1234@localhost:5432/mcpagent
DATABASE_URL="postgresql://sazib:1234@localhost:5432/mcpagent"

# ============================================
# AUTHENTICATION & SECURITY
# ============================================

# JWT Secret Key (Required in production, optional in development)
# Generate with: python3 backend/generate_keys.py
# Or: python3 -c 'import secrets; print(secrets.token_hex(32))'
# IMPORTANT: Change this to a strong, random string in production!
JWT_SECRET_KEY="your-secret-key-change-in-production-use-env-var"

# MCP API Key Encryption Key (Optional, but highly recommended)
# Generate with: python3 backend/generate_keys.py
# Or: python3 -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
# If not set, MCP API keys will be stored unencrypted in the database.
MCP_APIKEY_ENCRYPTION_KEY=""

# ============================================
# CORS & NETWORK
# ============================================

# CORS Origins
# Comma-separated list of allowed origins
# Example: "http://localhost:3000,http://localhost:8086,https://your-frontend.com"
CORS_ORIGINS="http://localhost:3000,http://localhost:8086,http://127.0.0.1:3000,http://127.0.0.1:8086"

# Server Port (Optional, defaults to 8000)
# PORT=8000

# ============================================
# LLM API KEYS (Optional - can be configured via frontend settings)
# ============================================
# If set here, they will be used as defaults or fallbacks.
# You can also configure them through the frontend Settings panel.

# OpenAI API Key
# Used for OpenAI models (gpt-4o, gpt-4, etc.) and embeddings
OPENAI_API_KEY=""

# Alternative OpenAI API Key (if different from OPENAI_API_KEY)
# OPENAI_LLM_API_KEY=""

# OpenAI Model (Optional, defaults to "gpt-4o")
# OPENAI_MODEL="gpt-4o"

# Google API Key (for Gemini models)
GOOGLE_API_KEY=""

# Groq API Key (for Groq models)
GROQ_API_KEY=""

# ============================================
# MCP SERVERS (Optional)
# ============================================
# MCP servers can be configured via the frontend Settings panel.
# This is only needed if you want to configure servers via environment variables.

# MCP_SERVERS='{"server1": {"url": "http://localhost:8000/mcp"}}'

# ============================================
# EXTERNAL SERVICES (Optional)
# ============================================

# Firecrawl API Key (for web scraping MCP server)
# FIRECRAWL_API_KEY=""

# ============================================
# ENVIRONMENT
# ============================================

# Environment (development, production, test)
# Used to determine if strict validation should be applied
ENVIRONMENT="development"

# Alternative environment variable (for compatibility)
# NODE_ENV="development"

# ============================================
# NOTES
# ============================================
# 1. Never commit .env file to version control
# 2. Use different keys for each environment (dev, staging, production)
# 3. Generate secure keys using: python3 backend/generate_keys.py
# 4. See KEY_SETUP.md for detailed key generation instructions
# 5. In production, JWT_SECRET_KEY and MCP_APIKEY_ENCRYPTION_KEY are required
